该阶段的功能为：派遣和发射最多四条重命名后的指令
派遣采用了纯组合逻辑，在一拍计算出指令会被送往哪一个发射队列
发射队列共四条：
前两条为算术指令的发射队列，用于发射算术指令，采用压缩的方式，为乱序发射，单个队列每周期最多发射一条指令，同时进行一次压缩操作。乱序发射采用了最简单的tomasulo算法。
后两条为乘除法和访存指令的发射队列，是普通的fifo，采用读写指针进行操作，为顺序发射。
握手信号的含义：
from_ready：发射队列有空位
to_valid：发射队列中存在指令，且指令有效，且指令没有数据相关

3.9更新：
添加了payloadRAM，其本质就是一份复制版的preg，可供发射队列中的指令读取数据。
inst_info新加了两项：
val data1 = UInt(32.W)
val data2 = UInt(32.W)
分别指示了preg1和preg2所对应的数据。
因为发射、压缩、读寄存器都放在了同一阶段（甚至是同一个周期），可以预见的是这个模块时序会非常糟糕。以后再说吧（挖坑）